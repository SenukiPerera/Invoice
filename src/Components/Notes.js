export default function Notes({notes}){
    return (
        <>
            <section className="note">
            {/*textarea*/}
            <p>{notes}</p>
          </section>
        </>
    )
}